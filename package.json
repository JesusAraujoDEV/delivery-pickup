{
  "name": "delivery-pickup",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "node src/server.js",
    "lint": "echo \"No linter configured\"",
    "db:test": "node src/scripts/test-aiven.js",
    "migrations:generate": "sequelize-cli migration:generate --name",
    "migrations:run": "npx sequelize-cli db:migrate --url \"%DB_URL%\" --migrations-path src/db/migrations",
    "migrations:revert": "npx sequelize-cli db:migrate:undo --url \"%DB_URL%\" --migrations-path src/db/migrations",
    "migrations:delete": "npx sequelize-cli db:migrate:undo:all --url \"%DB_URL%\" --migrations-path src/db/migrations",
    "test:integration": "node --test"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "csv-stringify": "^6.5.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "joi": "^17.12.3",
    "metrex": "^0.5.0",
    "pg": "^8.13.1",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.37.1",
    "swagger-ui-express": "^5.0.0",
    "yaml": "^2.6.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "sequelize-cli": "^6.6.3"
  }
}
